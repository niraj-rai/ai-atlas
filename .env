# Absolute site URL, used only for the Open Graph tags in index.html: social
# scrapers do not resolve relative image paths, so those must be fully
# qualified. Vite substitutes %VITE_SITE% at build time. No secrets here —
# override it for a different host.
VITE_SITE=https://niraj-rai.github.io/interactive-learning
